{# absences/reasons.html.twig #}

<div class="modal modal-lg" id="add-motif-modal" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="add-motif-modal-title" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="add-motif-modal-title">Liste des motifs d'absences</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">

        <p class="validateTips" style="margin-bottom: 6px;">Ajoutez, supprimez et modifiez l'ordre des motifs dans le menu déroulant.</p>
        <form id="add-reason">
          <div class="row">
            <input type="text" id="add-motif-text" class="col-6 form-control form-control-sm" style="width:300px;" required>
            <button type="button" id="add-motif-button" class="col-1 btn btn-primary" style="margin: 10px;font-size: 13px;">Ajouter</button>
          </div>
        </form>

        <form id="arrange-reasons">
          <fieldset class="container-fluid">
          <div class="row row-header">
              <i class="col-auto p-2 bi bi-arrow-down-up" aria-hidden="true"></i>
              <span class="col-3">Nom</span>
              <span class="col-3" >Niveau</span>
              <span class="col-3">Circuit de notification</span>
              <span class="col-2" style="text-align: center;">Télétravail</span>
          </div>
            <div id="motifs-sortable">
              {% if reasons is iterable %}
                {% for r in reasons %}
            
                  <li class="row row-motifs" id="li_{{ r.id }}">
                      <i class="col-auto p-2 bi bi-arrow-down-up"></i>
                      <span class="col-3 {% if r.type == 2 %} bold {% endif %}" id="valeur_{{ r.id}}">{{ r.valeur }}</span>

                    <div class="col-3">
                      <select id="type_{{ r.id }}" class="form-control form-select form-select-sm" aria-label="Séléction du Niveau" onchange="padding20($(this));">
                        {% for t in reason_types %}
                        <option value="{{ t.id }}" {% if t.id == r.type %} selected="selected" {% endif %}>{{ t.valeur }}</option>
                        {% endfor %}
                      </select>
                    </div>

                    <div class="col-3">
                      <select id="notification-workflow_{{ r.id }}" class="form-control form-select form-select-sm" aria-label="Séléction du circuit de notification" onchange="padding20($(this));">
                        <option value="A" {% if r.notification_workflow == 'A' %} selected="selected" {% endif %}>Circuit A</option>
                        <option value="B" {% if r.notification_workflow == 'B' %} selected="selected" {% endif %}>Circuit B</option>
                      </select>
                    </div>

                    <div class="col-2" style="text-align: center;">
                      <input type="checkbox" id="teleworking_{{ r.id }}" class="form-check-input" {% if r.teleworking %} checked="checked" {% endif %}/>
                    </div>

                    {% if r.unused %}
                      <span class="col-auto p-1" onclick="$(this).closest('li').hide();">
                        <i class="bi bi-trash3-fill"></i>
                      </span>
                    {% endif %}
                  </li>

                {% endfor %}
              {% endif %}
              </div>
          </fieldset>

        </form>

      </div>
      <div class="modal-footer">
        <button type="submit" class="btn btn-primary" form="arrange-reasons">Enregister</button>
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
      </div>
    </div>
  </div>
</div>