{# adminInfo/index.html.twig #}

{% extends 'base.html.twig' %}

{% block page %}
    <h3>Messages d'informations</h3>

    <div class='top-button-div'>
        <a href='{{ asset("admin/info/add") }}' class='ui-button ui-button-type1'>Ajouter</a>
    </div>

    {% if info %}
        <table class='CJDataTable' data-sort='[[1,"asc"],[2,"asc"]]' id='AbsenceInfoTable'>
            <thead>
                <tr>
                    <th class='dataTableNoSort tableSort'></th>
                    <th class="tableSort">Début</th>
                    <th class="tableSort">Fin</th>
                    <th class="tableSort">Texte</th>
                </tr>
            </thead>
            <tbody>
                {%for elem in info %}
                    <tr>
                        <td>
                            <a href='{{ asset('admin/info/') }}{{elem.id}}'>
                                <span class='pl-icon pl-icon-edit' title='Edit'></span>
                            </a>
                        </td>
                        <td>{{ elem.debut | date("d/m/Y") }}</td>
                        <td>{{ elem.fin | date("d/m/Y")  }}</td>
                        <td>{{ elem.texte }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% else %}
        <span class='no_information'>Aucune information enregistrée.</span>
    {% endif %}
{% endblock %}
